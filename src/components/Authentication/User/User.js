// Dependencies
import React from 'react';

// CSS
import styles from './User.module.css';

const User = () => {
    return (
        <div className={styles.User}>
            ---
        </div>
    );
};

export default User;